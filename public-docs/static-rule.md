-- Short --

A fixed numeric value that doesn't change over time. Simplest rule type.

-- Long --
# Static Number Rule

A fixed numeric value that doesn't change over time. The simplest rule type for fractal parameters.

Use this when you want a parameter to remain constant throughout the animation. Click the icon to convert to a range or step rule for dynamic values.

Perfect for setting baseline values like a specific escape radius or iteration count.
