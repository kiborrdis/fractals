---
applyTo: '**/*.tsx, **/*.ts, **/*.js'
---

- Always make sure code conforms to linting rules, has no type errors and formatted properly. Always use `yarn lint`,`yarn typecheck` and `yarn format` to do the check